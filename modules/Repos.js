import React from 'react'


export default React.createClass({
  render: function() {
    return (
      <div className="Repos">
        <h1>Repos for Repos</h1>
      </div>
    );
  }
});

// let mountPoint = document.getElementByID('root');
// ReactDom.render(<Repos>, mountpoint)
